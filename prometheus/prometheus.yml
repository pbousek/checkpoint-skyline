global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Prometheus prijima data vyhradne pres Remote Write od Check Point OpenTelemetry Collectoru.
# Endpoint: https://<tento-server>:9090/api/v1/write
# Vlastni scrape neni nakonfigurovan.

# Pokud se v budoucnu zapne Alertmanager, odkomentovat:
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets: ["localhost:9093"]

# rule_files:
#   - /etc/prometheus/rules/*.rules

scrape_configs: []
